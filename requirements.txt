# IPv6 Packet Processing Simulator - Requirements
# Core network and address analysis relies strictly on Python's built-in `ipaddress` library.

# Testing Framework
pytest>=8.0.0

# Interactive Web Application Framework (Phase 7)
streamlit>=1.30.0
